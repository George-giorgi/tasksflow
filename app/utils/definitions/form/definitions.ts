export type FormState = {
  name: string;
  surname: string;
  email: string;
  mobile: string | null;
};

export type FormStateTasks = {
  partNumber: string;
  description: string;
  metalType: string;
  drawing: string;
  qty: string;
  taskFor: string;
};
