export type FormState = {
  name: string;
  surname: string;
  email: string;
  mobile: string | null;
};

export type OneEmployee = {
  id: string;
  name: string;
  surname: string;
  email: string;
  mobile: string | null;
};
